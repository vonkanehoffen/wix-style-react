import Subheader from './Subheader';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(Subheader);

metadata.exportedFrom({
  path: 'src/Card/Subheader/Subheader.js',
});
