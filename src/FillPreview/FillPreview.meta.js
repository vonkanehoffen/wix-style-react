import FillPreview from './FillPreview';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(FillPreview);

metadata.exportedFrom({
  path: 'src/FillPreview/FillPreview.js',
});
