# Multi Area Drag and Drop List

An example usage of `droppable` `prop` in `<SortableList/>`.

Make sure you use the `<DragDropContextProvider/>` component to make it work.

Try to move items between the lists: