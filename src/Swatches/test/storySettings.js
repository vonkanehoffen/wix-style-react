import { Category } from '../../../stories/storiesHierarchy';

export const storySettings = {
  category: Category.COMPONENTS,
  storyName: 'Swatches',
  dataHook: 'story-color-swatches',
  testStoryNames: {
    DEFAULT: 'Default',
  },
};
