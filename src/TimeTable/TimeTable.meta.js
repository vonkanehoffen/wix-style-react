import TimeTable from './TimeTable';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(TimeTable);

metadata.exportedFrom({
  path: 'src/TimeTable/TimeTable.js',
});
