import ToggleSwitch from './ToggleSwitch';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(ToggleSwitch);

metadata.exportedFrom({
  path: 'src/ToggleSwitch/ToggleSwitch.js',
});
