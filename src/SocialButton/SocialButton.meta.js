import SocialButton from './SocialButton';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SocialButton);

metadata.exportedFrom({
  path: 'src/SocialButton/SocialButton.js',
});
