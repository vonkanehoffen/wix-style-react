import MediaOverlay from './MediaOverlay';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(MediaOverlay);

metadata.exportedFrom({
  path: 'src/MediaOverlay/MediaOverlay.js',
});
