import BrowserPreviewWidget from './BrowserPreviewWidget';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(BrowserPreviewWidget);

metadata.exportedFrom({
  path: 'src/BrowserPreviewWidget/BrowserPreviewWidget.js',
});
