import CustomModal from './CustomModal';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(CustomModal);

metadata.exportedFrom({
  path: 'src/CustomModal/CustomModal.js',
});
