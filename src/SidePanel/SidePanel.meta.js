import SidePanel from './SidePanel';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SidePanel);

metadata.exportedFrom({
  path: 'src/SidePanel/SidePanel.js',
});
