import Palette from './Palette';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(Palette);

metadata.exportedFrom({
  path: 'src/Palette/Palette.js',
});
