import VerticalTabs from './VerticalTabs';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(VerticalTabs);

metadata.exportedFrom({
  path: 'src/VerticalTabs/VerticalTabs.js',
});
